name: Bot Fotografia

on:
  schedule:
    - cron: '0 22 * * *'  # 19:00 Brasil (UTC-3)

  workflow_dispatch:
    inputs:
      test_mode:
        description: "Ativar modo teste? (true ou false)"
        required: false
        default: "false"
      test_modulo:
        description: "Módulo para teste (fundamentos, digital, analogica, etc)"
        required: false
        default: ""
      test_tema:
        description: "Tema específico para teste"
        required: false
        default: ""
      test_publicar:
        description: "Publicar ao vivo? (true ou false)"
        required: false
        default: "false"

permissions:
  contents: write

jobs:
  executar:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Código
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Atualizar pip
        run: python -m pip install --upgrade pip

      - name: Instalar Dependências
        run: pip install -r requirements.txt

      - name: Criar token.json a partir do Secret
        run: echo "${{ secrets.BLOGGER_TOKEN_BASE64 }}" | base64 --decode > token.json

      - name: Rodar Bot Fotografia
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
          UNSPLASH_API_KEY: ${{ secrets.UNSPLASH_API_KEY }}
          TEST_MODE: ${{ github.event.inputs.test_mode }}
          TEST_MODULO: ${{ github.event.inputs.test_modulo }}
          TEST_TEMA: ${{ github.event.inputs.test_tema }}
          TEST_PUBLICAR: ${{ github.event.inputs.test_publicar }}
        run: python run_bot.py

      - name: Commitar arquivos de controle (seguro)
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

          [ -f controle_diario.txt ] && git add controle_diario.txt
          [ -f controle_modulos.txt ] && git add controle_modulos.txt
          [ -f controle_temas.txt ] && git add controle_temas.txt
          [ -f controle_imagens.txt ] && git add controle_imagens.txt

          if git diff --staged --quiet; then
            echo "Nenhuma alteração para commit."
          else
            git commit -m "Atualiza arquivos de controle automaticamente"
            git push
          fi
